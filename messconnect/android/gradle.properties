org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
# PART 6: Kotlin incremental caches fail on this machine because the
# google_maps_flutter_android sources live on C: (pub cache) while the project
# is on D:; RelocatableFileToPathConverter can't compute a cross-drive path.
# Disabling incremental Kotlin compilation avoids the cache write entirely.
kotlin.incremental=false
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
